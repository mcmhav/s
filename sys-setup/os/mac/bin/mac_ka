#!/bin/bash

SLEEP_TIME=30

while true; do
  if [ -z "$GUARD_FROM" ] ||
    [ -z "$GUARD_TO" ] ||
    { [ "$(date +%H)" -gt "$GUARD_FROM" ] && [ "$(date +%H)" -lt "$GUARD_TO" ]; }; then
    osascript -e 'tell application "System Events" to key code 57'
  fi
  sleep $SLEEP_TIME
done
