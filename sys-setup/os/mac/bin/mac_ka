#!/bin/bash

if [ $(date +%H) -gt 8 ] && [ $(date +%H) -lt 18 ]; then
  loggit "lol"
fi

exit

while true; do
  echo "keeping alive sending code 57"
  osascript -e 'tell application "System Events" to key code 57'
  sleep 30
done
