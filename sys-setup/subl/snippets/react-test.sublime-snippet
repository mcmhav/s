<snippet>
    <content><![CDATA[
import React from 'react';
import { expect } from 'chai';
import { shallow, mount } from 'enzyme';
import sinon from 'sinon';

import ${1:componentName} from './${2:place}';

describe('${2:test-description}', () => {
    let componentMounted;

    before('mount render component', () => {
        componentMounted = mount(
            <${1:componentName}
                ${3:propName}=${4:propValue}
            />
        );
    });

    it('${5:test-name}', () => {
        console.log();
        console.log(componentMounted.debug());
        expect(componentMounted).to.contain();
    });
});
]]></content>
    <tabTrigger>react-test</tabTrigger>
    <scope>source.js</scope>
</snippet>
