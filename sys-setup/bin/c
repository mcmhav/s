#!/bin/bash

c() {
  if [ "$(uname -s)" == "Linux" ]; then
    echo "todo"
  elif [ "$(uname -s)" == "Darwin" ]; then
    "$1" | pbcopy
  elif [ "$(uname -s)" == "MINGW64_NT-10.0" ]; then
    echo "todo"
  fi
}

c $1
