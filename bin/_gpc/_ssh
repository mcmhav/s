#!/usr/bin/env bash

SCRIPT_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
source "$SCRIPT_PATH/_env"

# workspace list | awk 'NR>1 {print $2}' | sort -u
"$SCRIPT_PATH/gitpod" workspace list | fzf
