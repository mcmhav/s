#!/usr/bin/env bash

VIM_EXEC="/usr/bin/vim"

case $1 in
+Plug*) ;;
*)
	if command -v nvim 1>/dev/null 2>&1; then
		VIM_EXEC="nvim"
	fi
	;;
esac

echo "Using $VIM_EXEC as vim executable."
"$VIM_EXEC" "$@"
