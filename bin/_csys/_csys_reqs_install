#!/usr/bin/env bash

REQS_FILE_PATH="$1"

REQS_COUNT=$(wc -l "$REQS_FILE_PATH" | awk '{print $1}')

loggit "Reqs to setup: $REQS_COUNT"

while read -ra l; do
  csys install "${l[@]}"
done <"$REQS_FILE_PATH"
