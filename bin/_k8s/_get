#!/usr/bin/env bash

RESOURCE="${1:-pod}"

kubectl get "$RESOURCE" -A | fzf \
  --preview "kubectl get $RESOURCE {+2} -n {+1} -o yaml" \
  --bind=ctrl-j:preview-down \
  --bind=ctrl-k:preview-up \
  --bind=ctrl-d:preview-half-page-down \
  --bind=ctrl-u:preview-half-page-up
